export class Vehicle {
    name: string;
    total_no: number;
    max_distance: number;
    speed: number;
}

export class VehicleDisplay {
    vehicle: Vehicle;
    available: number;
    disabled: boolean;
}